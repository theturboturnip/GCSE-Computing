#include <stdio.h> 

int fibbonaci(int x){
    int first=0,second=0,current=0,i=0;
    for(i=0;i<x;i++){
        if(first==0||second==0){
            current=1;
        }
        first=second;
        second=current;
        current=first+second;
    }
    return current;
}
int main(){
    printf("%d",fibbonaci(5));
}
